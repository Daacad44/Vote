import type { PropsWithChildren } from "react";  type Props = PropsWithChildren<{   title: string;   subtitle?: string;   actions?: React.ReactNode;   icon?: string; }>;  export function SectionCard({ title, subtitle, actions, children, icon = "?" }: Props) {   return (     <section className="sparkle-border">       <div className="rounded-[1.9rem] border border-white/70 bg-white/95 p-6 shadow-[0_25px_50px_rgba(15,23,42,0.1)]">         <div className="mb-4 flex items-start justify-between gap-4">           <div className="flex items-start gap-3">             <span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-slate-100 text-lg">{icon}</span>             <div>               <h2 className="text-xl font-semibold text-slate-900">{title}</h2>               {subtitle && (                 <p className="text-sm text-slate-500">{subtitle}</p>               )}             </div>           </div>           {actions && <div className="text-sm">{actions}</div>}         </div>         {children}       </div>     </section>   ); } 
